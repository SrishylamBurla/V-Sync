import asyncHandler from "express-async-handler";
import Invoice from "../models/Invoice.js";
import Payment from "../models/Payment.js";
import Refund from "../models/Refund.js";
import Patient from "../models/Patient.js";
import Branch from "../models/Branch.js";
const branch=async org=>Branch.findOne({organizationId:org,status:"active"}).sort({createdAt:1});
const number=(prefix)=>`${prefix}-${Date.now().toString().slice(-9)}`;
const recalc=invoice=>{const lines=(invoice.items||[]).reduce((sum,i)=>sum+(Number(i.quantity||0)*Number(i.unitPrice||0)-Number(i.discount||0)),0);const subtotal=Math.max(0,lines);invoice.subtotal=subtotal;invoice.total=Math.max(0,subtotal-Number(invoice.discount||0)+Number(invoice.tax||0));invoice.balance=Math.max(0,invoice.total-Number(invoice.paidAmount||0));invoice.status=invoice.balance<=0?"paid":Number(invoice.paidAmount||0)>0?"partially_paid":invoice.status==="draft"?"draft":"issued";return invoice;};
const populate=Invoice.find.bind(Invoice);
export const listInvoices=asyncHandler(async(req,res)=>{const q={organizationId:req.user.organizationId};if(req.query.status)q.status=req.query.status;if(req.query.patientId)q.patientId=req.query.patientId;const list=await Invoice.find(q).populate("patientId","patientNumber firstName middleName lastName phone").sort({invoiceDate:-1,createdAt:-1}).limit(250).lean();res.json({success:true,data:list})});
export const getInvoice=asyncHandler(async(req,res)=>{const invoice=await Invoice.findOne({_id:req.params.id,organizationId:req.user.organizationId}).populate("patientId","patientNumber firstName middleName lastName phone email address").populate("createdBy","firstName lastName");if(!invoice){res.status(404);throw new Error("Invoice not found")}const payments=await Payment.find({invoiceId:invoice._id,organizationId:req.user.organizationId}).sort({paymentDate:-1}).populate("createdBy","firstName lastName").lean();const refunds=await Refund.find({invoiceId:invoice._id,organizationId:req.user.organizationId}).sort({refundDate:-1}).lean();res.json({success:true,data:{invoice,payments,refunds}})});
export const createInvoice=asyncHandler(async(req,res)=>{const p=await Patient.findOne({_id:req.body.patientId,organizationId:req.user.organizationId,status:"active"});if(!p){res.status(404);throw new Error("Patient not found")}const b=await branch(req.user.organizationId);if(!b){res.status(400);throw new Error("An active practice branch is required")}const invoice=new Invoice({organizationId:req.user.organizationId,branchId:b._id,patientId:p._id,invoiceNumber:number("INV"),invoiceDate:req.body.invoiceDate||new Date(),dueDate:req.body.dueDate,items:req.body.items||[],discount:Number(req.body.discount||0),tax:Number(req.body.tax||0),notes:req.body.notes,createdBy:req.user._id,status:req.body.status||"issued"});recalc(invoice);await invoice.save();res.status(201).json({success:true,message:"Invoice created successfully",data:invoice})});
export const updateInvoice=asyncHandler(async(req,res)=>{const invoice=await Invoice.findOne({_id:req.params.id,organizationId:req.user.organizationId});if(!invoice){res.status(404);throw new Error("Invoice not found")}if(["paid","void","refunded"].includes(invoice.status)&&req.body.items){res.status(400);throw new Error("Finalized invoices cannot have their items changed")};for(const k of ["items","discount","tax","dueDate","notes"]){if(req.body[k]!==undefined)invoice[k]=req.body[k]}recalc(invoice);await invoice.save();res.json({success:true,message:"Invoice updated",data:invoice})});
export const addPayment=asyncHandler(async(req,res)=>{const invoice=await Invoice.findOne({_id:req.params.id,organizationId:req.user.organizationId});if(!invoice){res.status(404);throw new Error("Invoice not found")}const amount=Number(req.body.amount);if(!amount||amount<=0)throw new Error("Payment amount must be greater than zero");if(amount>invoice.balance+0.001)throw new Error("Payment exceeds outstanding balance");const payment=await Payment.create({organizationId:req.user.organizationId,branchId:invoice.branchId,patientId:invoice.patientId,invoiceId:invoice._id,receiptNumber:number("RCT"),paymentDate:req.body.paymentDate||new Date(),method:req.body.method||"cash",amount,reference:req.body.reference,notes:req.body.notes,createdBy:req.user._id});invoice.paidAmount=Number(invoice.paidAmount||0)+amount;recalc(invoice);await invoice.save();res.status(201).json({success:true,message:"Payment recorded successfully",data:{payment,invoice}})});
export const addRefund=asyncHandler(async(req,res)=>{const invoice=await Invoice.findOne({_id:req.params.id,organizationId:req.user.organizationId});if(!invoice){res.status(404);throw new Error("Invoice not found")}const amount=Number(req.body.amount);if(!amount||amount<=0)throw new Error("Refund amount must be greater than zero");const already=(await Refund.aggregate([{$match:{invoiceId:invoice._id,organizationId:req.user.organizationId}},{$group:{_id:null,total:{$sum:"$amount"}}}]))[0]?.total||0;if(amount>Number(invoice.paidAmount||0)-already+0.001)throw new Error("Refund exceeds refundable amount");const refund=await Refund.create({organizationId:req.user.organizationId,branchId:invoice.branchId,patientId:invoice.patientId,invoiceId:invoice._id,refundNumber:number("REF"),refundDate:req.body.refundDate||new Date(),amount,method:req.body.method||"cash",reason:req.body.reason||"Customer refund",createdBy:req.user._id});if(already+amount>=Number(invoice.paidAmount||0))invoice.status="refunded";await invoice.save();res.status(201).json({success:true,message:"Refund recorded successfully",data:{refund,invoice}})});
export const summary=asyncHandler(async(req,res)=>{const base={organizationId:req.user.organizationId};const [issued,paid,outstanding]=await Promise.all([Invoice.aggregate([{$match:base},{$group:{_id:null,total:{$sum:"$total"}}}]),Invoice.aggregate([{$match:base},{$group:{_id:null,total:{$sum:"$paidAmount"}}}]),Invoice.aggregate([{$match:base},{$group:{_id:null,total:{$sum:"$balance"}}}])]);res.json({success:true,data:{invoiced:issued[0]?.total||0,paid:paid[0]?.total||0,outstanding:outstanding[0]?.total||0}})});
